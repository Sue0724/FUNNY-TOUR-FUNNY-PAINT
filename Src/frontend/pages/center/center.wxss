page {
  padding-bottom: 50rpx;
  background: -webkit-linear-gradient(top, #82af8a 0%, #CCCCCC 100%);
  background: -o-linear-gradient(top, #82af8a 0%, #CCCCCC 100%);
  background: -ms-linear-gradient(top, #82af8a 0%, #CCCCCC 100%);
  /* background: linear-gradient(to bottom, #a7d4c3 0%, #ffffff 100%); */
  background: linear-gradient(to bottom, #82af8a 0%, #ffffff 100%);
  /* background: linear-gradient(to bottom, #bae4f7 0%, #ffffff 100%); */
  /* background: linear-gradient(to bottom, #7db0da 0%, #ffffff 100%); */
  /*配色直接找现成的*/
}

.header {
  padding: 0 25rpx;
  padding-top: 30rpx;
  padding-bottom: 50rpx;
  width: 100%;
}

.iimg {
  width: 45rpx;
  height: 45rpx;
  margin-right: 10rpx;
  top: 10rpx;
  position: relative;
}

.user_box {
  display: flex;
  align-items: center;
  border-radius: 30px;/*边框圆角*/
  /* width: calc(94%- 40px); 减去增加的间距再*96%，保证跟nav项同宽
   */
  width: 94%;
  height: 150px;
  background: rgba(255, 255, 255, 0.7);
  
  /* padding-left: 20px; 添加左内边距，防止太靠边 */
  /* padding: 0 20px; 左右均添加内边距 */
}

.logo {
  width: 150rpx; /* 确保保持一致 */
  height: 150rpx; /* 确保保持一致 */
  border-radius: 50%; /* 保持圆形 */
  /* background: #fff;??? */
  background: rgba(255, 255, 255, 0.7);
  overflow: hidden; /* 防止内容溢出 */
  display: flex; /* 使用 flex 来中心对齐图片 */
  justify-content: center;
  align-items: center;
  margin-right: 40px; /*添加右边距*/
  margin-left: 20px;
}

.logo image {
  width: 100%; /* 占满父容器 */
  height: 100%; /* 占满父容器 */
  object-fit: cover; /* 保持图片比例，防止拉伸 */
  border-radius: 50%; /* 确保图片为圆形 */
}

.user_name {
  display: flex;
  align-items: center;
  font-size: 36rpx;
  font-weight: bold;
  /* color: #fff; ???*/
  color: rgba(255, 255, 255, 0.7);
}


.user_name image {
  margin-left: 16rpx;
  width: 84rpx;
  height: 31rpx;
}

.headname {
  /* margin-left: 3px; */
  /* margin-top: 2px; */
  /* padding-right: 70px; */
  /*这里改位置尝试*/
  /* padding-left: 0px; */
  padding-right: 30px;
  padding-top: 0px;
  font-size: 20px;
}

.briefintr{
  /* padding-left: 0px; */
  padding-right: 30px;
  /* padding-bottom: 5px; */
  padding-top: 10px;
  font-size: 12px;
}
.nav {
  margin-top: 40px;
  font-size: 40rpx;
  background: rgba(255, 255, 255, 0.7);
  width: 94%;
  border-radius: 30px;/*边框圆角*/
  overflow: hidden; /* 确保圆角效果不会被边框覆盖 */
  box-shadow: 0 1px 0 0 #e5e5e5; /* 模拟底部边框 */
}

.nav .item {
  display: flex;
  justify-content: space-between;
  border-bottom: 1px solid #e5e5e5;
  padding: 50rpx 0;
}

.avatar {
  width: 100%; /* 调整头像的宽度为250px */
  height: 100%; /* 调整头像的高度为250px */
  border-radius: 50%; /* 保持圆形 */
  overflow: hidden;
  object-fit: cover; /* 保持图片比例，防止拉伸 */
}

